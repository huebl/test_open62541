
Create Certificate Signing Request
==================================

openssl req -nodes -new -newkey rsa:2048 -sha256 -out csr.pem


Show Certificate Signing Request
================================

openssl req -noout -text -inform der -in csr_openssl.der

